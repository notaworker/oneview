name: Schedule Test Hourly

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Print UTC time
        run: echo "Schedule fired: $(date -u)"
